-- Migration v1 -> v2: add the app_setting key/value table (theme preference, etc.).
-- Existing databases (created before this table existed) run this on next launch;
-- fresh installs get the table from Settings.sq's CREATE TABLE directly.

CREATE TABLE app_setting (
    key   TEXT NOT NULL PRIMARY KEY,
    value TEXT NOT NULL
);
