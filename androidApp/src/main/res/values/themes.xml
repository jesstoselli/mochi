<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- windowBackground is what the system draws during cold start. It points at a color with
         a values-night variant, so it follows the system light/dark setting. -->
    <style name="Theme.Mochi" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- Android 12+ splash (with backport via core-splashscreen): the mochi on a cream badge,
         centered on the theme-aware background. Hands off to Theme.Mochi once the app is ready. -->
    <style name="Theme.Mochi.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Mochi</item>
    </style>
</resources>